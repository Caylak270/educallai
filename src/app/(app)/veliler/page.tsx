import { PageStub } from "@/components/shell/page-stub";

export const metadata = { title: "Veliler (CRM)" };

export default function Page() {
  return <PageStub title="Veliler (CRM)" icon="contacts" designFile="design/screens/01-veliler-crm.mobile.html" />;
}
